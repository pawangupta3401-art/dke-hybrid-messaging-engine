# Git Push Rule

After implementing, editing, or verifying any code changes in this repository:
1. Stage all relevant changes: `git add .`
2. Create a concise, meaningful commit message: `git commit -m "..."`
3. Push to origin main: `git push origin main`
